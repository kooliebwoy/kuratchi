---
title: "Routing"
description: "File-based routing, layouts, template syntax, and client reactivity"
---

## Route conventions

Place `.html` files inside `src/routes/`. File paths become URL patterns.

```text
src/routes/index.html              -> /
src/routes/about/index.html        -> /about
src/routes/items/index.html        -> /items
src/routes/blog/[slug]/index.html  -> /blog/:slug
src/routes/layout.html             -> shared layout
```

## Route execution model

Kuratchi pages are SSR by default, but the top-level route `<script>` is your browser/client script.

That means you can use browser code there like DOM access, event handlers, local state, and client-side imports.

For server work, use `$server/` imports, actions, and runtime hooks.

```html
<script>
  import { getItems } from '$server/items';
  const items = await getItems();
</script>

<ul>
  for (const item of items) {
    <li>{item.title}</li>
  }
</ul>
```

This is especially important for `$server/` imports. When you `await` a `$server/` function at the top level, Kuratchi runs that read during SSR and keeps your code in the same order you wrote it.

Use `src/server` for private modules and backend logic.

## Layouts

`src/routes/layout.html` wraps every page. Use `<slot></slot>` where route content should render.

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <title>My App</title>
</head>
<body>
  <nav>
    <a href="/">Home</a>
    <a href="/items">Items</a>
  </nav>
  <main>
    <slot></slot>
  </main>
</body>
</html>
```

## Template syntax

### Interpolation

```html
<p>{title}</p>
<p>{@html bodyHtml}</p>
<p>{@raw trustedHtml}</p>
```

### Conditionals

```html
if (items.length === 0) {
  <p>Nothing here yet.</p>
} else {
  <p>{items.length} items</p>
}
```

### Loops

```html
for (const item of items) {
  <li>{item.title}</li>
}
```

### Components

```html
<script>
  import Card from '$lib/card.html';
  import Badge from '@kuratchi/ui/badge.html';
</script>

<Card title="Stack">
  <Badge variant="success">Live</Badge>
</Card>
```

### Component Props

Components receive props via a `props` object. Destructure in the component's `<script>` block:

```html
<!-- src/lib/card.html -->
<script>
  const { title, class: className = '', variant = 'default' } = props;
</script>

<div class="card {className}" data-variant={variant}>
  if (title) {
    <h2>{title}</h2>
  }
  <slot></slot>
</div>
```

**Props patterns:**
- Destructure from `props` with defaults: `const { title, size = 'md' } = props;`
- Access directly in template: `{props.title}` or `data-variant={props.variant}`
- Use `class:` for className (reserved word): `const { class: className = '' } = props;`
- Children go in `<slot></slot>` (renders as `props.children`)

## Client reactivity

Use `$:` inside browser-side template scripts when you need client updates:

```html
<script>
  let users = ['Alice'];

  $: console.log(`Users: ${users.length}`);

  function addUser() {
    users.push('Bob');
  }
</script>
```

Rules:

- The top route `<script>` is browser/client code.
- `$:` is available when you want reactive browser updates.
- Proxy-backed array and object mutation works when `$:` is active.
- You should not need browser guards in normal route code.
